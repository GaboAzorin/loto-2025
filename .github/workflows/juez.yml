name: Juez Implacable (Auditoría)

on:
  push:
    paths:
      - 'LOTO_HISTORIAL_MAESTRO.csv' # Se activa automático si subes resultados
  workflow_dispatch: # <--- ESTO HABILITA EL BOTÓN MANUAL

jobs:
  judge:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Permiso para actualizar el CSV

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        pip install pandas numpy

    - name: Run Juez Implacable
      run: python juez_implacable.py

    - name: Commit and Push Results
      run: |
        git config --global user.name 'Juez LotoAI'
        git config --global user.email 'juez@lotoai.com'
        git pull
        git add LOTO_SIMULACIONES.csv
        git commit -m "⚖️ Auditoría completada (Recálculo)" || exit 0
        git push